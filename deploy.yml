name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t soniagurram/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true soniagurram/react-test npm test

      - run: docker build -t soniagurram/multi-client ./client
      - run: docker build -t soniagurram/multi-nginx ./nginx
      - run: docker build -t soniagurram/multi-server ./server
      - run: docker build -t soniagurram/multi-worker ./worker

      - run: docker push soniagurram/multi-docker/multi-client
      - run: docker push soniagurram/multi-docker/multi-nginx
      - run: docker push soniagurram/multi-docker/multi-server
      - run: docker push soniagurram/multi-docker/multi-worker
